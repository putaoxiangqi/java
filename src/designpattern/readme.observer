

观察者模式:
   定义了对象之间1对多的依赖关系，主题是1，多是多个观察者，
   当主题对象的数据变更时，所有的依赖着都会受到通知并且自动更新。

观察者模式的设计目的:
    松耦合，对象之间可以交互，但是不需要太清楚彼此之间的细节


报纸出版社: 即为主题(Subject)

报纸订阅者:  即为观察者(Observer)


主题对象管理某些数据，当主题中数据变更时就通知订阅者


逻辑:

1. 订阅(订阅类的构造函数入参是主题)
   鸭子对象过来告诉主题，它想当一个观察者

2. 鸭子成为订阅者
   实例化的时候，订阅类的构造函数入参是主题，直接调用register函数


3. 主题有了新的数据值，通知所有观察者
   调用订阅者的update